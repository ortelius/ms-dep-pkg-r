apiVersion: v1
kind: Service
metadata:
  name: {{ include "ms-dep-pkg-r.name" . }}
spec:
  selector:
    app: {{ include "ms-dep-pkg-r.name" . }}
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: NodePort
---
